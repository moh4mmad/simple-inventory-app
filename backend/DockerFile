FROM node:10
ENV DBHOST=${DBHOST}
ENV DBUSER=${DBUSER}
ENV DBPASS=${DBPASS}
ENV DBNAME=${DBNAME}
WORKDIR /app
COPY package*.json ./
RUN npm install 
COPY . .
EXPOSE 8080
RUN mkdir -p /app/uploads
CMD ["npm", "run", "start:production"]
